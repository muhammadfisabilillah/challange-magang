<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard Admin</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    
    <style>
        body { background-color: #f4f6f9; }
        
        /* Kartu Statistik dengan Gradasi Halus */
        .card-stat {
            border: none;
            border-radius: 15px;
            color: white;
            transition: transform 0.3s;
            overflow: hidden;
            position: relative;
        }
        .card-stat:hover { transform: translateY(-5px); }
        
        /* Variasi Warna Gradasi */
        .bg-gradient-purple { background: linear-gradient(45deg, #6a11cb, #2575fc); }
        .bg-gradient-orange { background: linear-gradient(45deg, #ff9966, #ff5e62); }
        .bg-gradient-green { background: linear-gradient(45deg, #11998e, #38ef7d); }

        .stat-icon {
            font-size: 3rem;
            opacity: 0.3;
            position: absolute;
            right: 20px;
            top: 10px;
        }

        .welcome-card {
            background: white;
            border-radius: 15px;
            border: none;
            box-shadow: 0 5px 20px rgba(0,0,0,0.05);
        }
    </style>
</head>
<body>

    <nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4 shadow-sm">
        <div class="container">
            <a class="navbar-brand fw-bold" href="#">
                <i class="bi bi-rocket-takeoff"></i> Admin Panel
            </a>
            <div class="d-flex">
                <span class="navbar-text text-light me-3">
                    Halo, <strong>{{user.name}}</strong>
                </span>
                <a href="/auth/login" class="btn btn-danger btn-sm rounded-pill px-3">
                    Logout <i class="bi bi-box-arrow-right ms-1"></i>
                </a>
            </div>
        </div>
    </nav>

    <div class="container">
        
        <div class="row mb-4">
            <div class="col-12">
                <div class="card welcome-card p-4">
                    <div class="d-flex align-items-center">
                        <div class="flex-grow-1">
                            <h2 class="fw-bold text-dark">Selamat Datang Kembali! 👋</h2>
                            <p class="text-muted mb-0">Ini adalah pusat kendali aplikasi magang kamu. Semua sistem berjalan normal.</p>
                        </div>
                        <div class="d-none d-md-block">
                            <i class="bi bi-pc-display-horizontal text-primary" style="font-size: 3rem;"></i>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div class="row mb-4">
            
            <div class="col-md-4 mb-3">
                <div class="card card-stat bg-gradient-purple p-3 shadow">
                    <div class="card-body">
                        <h5 class="card-title text-uppercase mb-0">Total Produk</h5>
                        <h1 class="display-4 fw-bold mb-0">{{stats.products}}</h1>
                        <small>Item terdaftar</small>
                        <i class="bi bi-box-seam stat-icon"></i>
                    </div>
                </div>
            </div>

            <div class="col-md-4 mb-3">
                <div class="card card-stat bg-gradient-orange p-3 shadow">
                    <div class="card-body">
                        <h5 class="card-title text-uppercase mb-0">Total Kategori</h5>
                        <h1 class="display-4 fw-bold mb-0">{{stats.categories}}</h1>
                        <small>Jenis varian</small>
                        <i class="bi bi-tags stat-icon"></i>
                    </div>
                </div>
            </div>

            <div class="col-md-4 mb-3">
                <div class="card card-stat bg-gradient-green p-3 shadow">
                    <div class="card-body">
                        <h5 class="card-title text-uppercase mb-0">Status Sistem</h5>
                        <h1 class="display-6 fw-bold mb-0 mt-2">ONLINE 🟢</h1>
                        <small>Server NestJS Active</small>
                        <i class="bi bi-hdd-network stat-icon"></i>
                    </div>
                </div>
            </div>
        </div>

        <h5 class="fw-bold text-secondary mb-3"><i class="bi bi-grid-fill"></i> Menu Cepat</h5>
        <div class="row">
            <div class="col-md-6 mb-3">
                <a href="/products" class="text-decoration-none">
                    <div class="card shadow-sm border-0 h-100 hover-shadow">
                        <div class="card-body d-flex align-items-center p-4">
                            <div class="bg-primary bg-opacity-10 p-3 rounded-circle me-3">
                                <i class="bi bi-cart-check text-primary fs-3"></i>
                            </div>
                            <div>
                                <h5 class="fw-bold text-dark mb-1">Kelola Produk</h5>
                                <p class="text-muted small mb-0">Tambah, Edit, Cari dan Hapus Produk.</p>
                            </div>
                            <i class="bi bi-chevron-right ms-auto text-muted"></i>
                        </div>
                    </div>
                </a>
            </div>

            <div class="col-md-6 mb-3">
                <a href="/categories" class="text-decoration-none">
                    <div class="card shadow-sm border-0 h-100 hover-shadow">
                        <div class="card-body d-flex align-items-center p-4">
                            <div class="bg-warning bg-opacity-10 p-3 rounded-circle me-3">
                                <i class="bi bi-list-stars text-warning fs-3"></i>
                            </div>
                            <div>
                                <h5 class="fw-bold text-dark mb-1">Kelola Kategori</h5>
                                <p class="text-muted small mb-0">Atur jenis dan varian produk disini.</p>
                            </div>
                            <i class="bi bi-chevron-right ms-auto text-muted"></i>
                        </div>
                    </div>
                </a>
            </div>
        </div>

    </div>

</body>
</html>